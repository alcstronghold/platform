language: typescript
type: library

tasks:
  build:
    command: tsc -b
    inputs:
      - src/**/*
      - tsconfig.json
    outputs:
      - dist/**/*

  typecheck:
    command: tsc --noEmit
    inputs:
      - src/**/*
      - tsconfig.json
