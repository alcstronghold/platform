language: typescript
type: library

tasks:
  test:
    command: "bunx vitest run"
    inputs:
      - src/**/*
      - vitest.config.ts
      - vitest.setup.ts

  typecheck:
    command: bun run tsc --noEmit
    inputs:
      - src/**/*
      - tsconfig.json
